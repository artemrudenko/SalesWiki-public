"""Permissioned-knowledge core service.

A separable core (identity, boundaries, policy, retrieval, formatter, audit,
proposals) so the MCP gateway is a thin wrapper and the logic stays unit- and
role-testable. See docs/engineering/permissioned-knowledge-architecture.md.
"""

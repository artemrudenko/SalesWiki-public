# Local synthetic-demo identity. Copy to integrations/workbench/.env locally;
# never commit the copied file. This is not a password or a production login.
SALESWIKI_DEMO_ACTOR=demo-sophie-curator

# The BFF is intentionally loopback-only. For a LAN presentation, expose only
# the UI development server; its same-origin proxy continues to reach the BFF
# on localhost.
